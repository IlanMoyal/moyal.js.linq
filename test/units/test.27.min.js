/*!
 * File: test/units/test.27.min.js
 */

/* eslint-disable no-unused-vars */

import { TestGroup } from "@moyal/js-test";
import Linq from "../../src/index.js";
import Errors from "../../src/errors.js";
import {} from "./globals.js";

let tempNumbersArr; /* to be used within tests using comma separator */
let tempString = null;
let tempValue;

export default new TestGroup("min() function")
	.throws("", () => new Linq([1, 3, 5, -6, 4, 3, 11, 0, -87]).min(11), e => e.message === Errors.Messages.MUST_BE_FUNCTION_OR_NULLISH_comparer)
	.throws("", () => new Linq([1, 3, 5, -6, 4, 3, 11, 0, -87]).min({}), e => e.message === Errors.Messages.MUST_BE_FUNCTION_OR_NULLISH_comparer)
	.throws("", () => new Linq([1, 3, 5, -6, 4, 3, 11, 0, -87]).min("abc"), e => e.message === Errors.Messages.MUST_BE_FUNCTION_OR_NULLISH_comparer)
	.areEqual("", -87, new Linq([1, 3, 5, -6, 4, 3, 11, 0, -87]).min())
	.areEqual("", -6, new Linq([1, 3, 5, -6, 4, 3, 11, 0, -87]).min((a, b, bIndex) => a % 2 === 0 && b % 2 !== 0 ? -1 : (a % 2 !== 0 && b % 2 === 0 ? 1 : a - b)))
	.areEqual("", -87, new Linq([1, 3, 5, -6, 4, 3, 11, 0, -87, 100, 52, 33]).min())
	.areEqual("", -6, new Linq([1, 3, 5, -6, 4, 3, 11, 0, -87, 100, 52, 33]).min((a, b, bIndex) => a % 2 === 0 && b % 2 !== 0 ? -1 : (a % 2 !== 0 && b % 2 === 0 ? 1 : a - b)))
	.areEqual("", 2, new Linq([2, 5]).min())
	.areEqual("", 2, new Linq([2, 5]).min((a, b, bIndex) => a % 2 === 0 && b % 2 !== 0 ? -1 : (a % 2 !== 0 && b % 2 === 0 ? 1 : a - b)))
	.areEqual("", 5, new Linq([12, 5]).min())
	.areEqual("", 12, new Linq([12, 5]).min((a, b, bIndex) => a % 2 === 0 && b % 2 !== 0 ? -1 : (a % 2 !== 0 && b % 2 === 0 ? 1 : a - b)))
	.areEqual("", 12, new Linq([12]).min())
	.areEqual("", 12, new Linq([12]).min((a, b, bIndex) => a % 2 === 0 && b % 2 !== 0 ? -1 : (a % 2 !== 0 && b % 2 === 0 ? 1 : a - b)))
	.areEqual("", undefined, new Linq([]).min())
	.areEqual("", undefined, new Linq([]).min((a, b, bIndex) => a % 2 === 0 && b % 2 !== 0 ? -1 : (a % 2 !== 0 && b % 2 === 0 ? 1 : a - b)));
	